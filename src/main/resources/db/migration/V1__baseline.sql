-- ============================================
-- V1: Baseline migration
-- Marks existing schema as the starting point
-- for Flyway. All prior DDL is assumed to exist.
-- ============================================
-- No DDL changes — baseline only
SELECT 1;
